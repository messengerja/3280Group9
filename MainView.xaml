<Window x:Class="FinalAssignment.Views.MainView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:local="clr-namespace:FinalAssignment.Views"
            mc:Ignorable="d" 
             Title="MainView" Height="350" Width="525">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        <Button Name="btnOrders" Grid.Column="1" Grid.Row="2" Content="Orders" Margin="5" Command="{Binding OrdersCommand}"/>
        <Button Name="btnInventory" Grid.Column="2" Grid.Row="2" Content="Inventory" Margin="5" Command="{Binding InventoryCommand}"/>
        <Button Name="btnNewOrders" Grid.Column="3" Grid.Row="2" Content="New Orders" Margin="5" Command="{Binding NewOrderCommand}"/>
    </Grid>
</Window>
